/**
 * @param {string} s
 * @param {string} t
 * @return {boolean}
 */
var isAnagram = function(s, t) {
    let x1=s.split('').sort().join('')
    let x2=t.split('').sort().join('')
    if(x1==x2) return true
    else return false
};
